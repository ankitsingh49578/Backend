<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h3>All Chats!</h3>
    <form method="GET" action="/chats/new">
        <button>New Chat</button>
    </form>
    <%for(let chat of chats){ %>
        <div class="chats">
            <p><b>From:</b> <i><%= chat.from %></i></p>
            <p class="Received"><b>Received by:</b> <i><%= chat.to %></i></p>
            <div class="msg">
                <p><b>Message:</b> <i><%= chat.message %></i></p>
                <form method="GET" action="/chats/<%= chat._id %>/edit">
                    <button>Edit Chat</button>
                </form>
            </div>
            <p><b>Time:</b> <i><%= chat.created_at.toString().split(" ")[4] %></i></p>
            <p><b>Date:</b> <i><%= chat.created_at.toString().split(" ").slice(0, 4).join("-") %></i></p>
            <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE">
                <button>Delete</button>
            </form>
        </div>
        <br>
    <%}%>
</body>
</html>